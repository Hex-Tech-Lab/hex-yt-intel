auto-install-peers=true
frozen-lockfile=true
# pnpm v11: allow builds for native dependencies
# @sentry/cli, esbuild, sharp, unrs-resolver, workerd require compilation
# ignore-scripts disabled to permit authorized build scripts
