node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
.turbo/
.next/
.env*.local
test-results/
playwright-report/
docs/specs/.env_*
.env*

# Windows/WSL filesystem metadata
*:Zone.Identifier
# Claude Code agent worktrees / local state (never commit agent scratch space)
.claude/
.kilo/
# Cloudflare Wrangler local build artifacts
.wrangler/
# Binary design-system exports (source-of-truth lives as extracted .tsx/.css)
design-system/*.zip

# Private agent skills
.gemini/skills/
docs/skills/
.agents/

# Local-only sharing files
docs/for_sharing/

# Confidential/strategic docs (business decisions, pricing, payment provider
# selection, launch strategy, ADRs with commercial/legal reasoning) --
# per global CLAUDE.md Rule #0: never committed to a public repo, local disk
# only. Directive 2026-08-08.
docs/private/

# Dataset assets (local-only calibration — never commit large CSV corpora)
scripts/quality-engine/calibration/data/
.qa-intel/baseline.json

# Supabase CLI local branch-tracking state (created by `supabase db start`/`reset`)
supabase/.branches/
# Added by code-review-graph
.code-review-graph/
testsprite_tests/tmp/
testsprite_tests/tmp/config.json
