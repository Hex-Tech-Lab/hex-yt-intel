{
  "name": "youtube-intelligence-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for YouTube metadata fetching",
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/worker.ts --bundle --format=esm --outfile=dist/worker.js",
    "test": "echo \"No tests yet\""
  },
  "keywords": ["cloudflare", "worker", "youtube"],
  "author": "CCW",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250203.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.90.0"
  }
}
