# Architecture Decision Records

## Format
`[YYYY-MM-DD] [AgentID] [Status] [DECISION] Title. Rationale: ... Alternatives: ... Confirmed by user: yes/no`
Status: ACTIVE | SUPERSEDED | ✅

---
- [2026-06-18] [OCT2] [ACTIVE] [DECISION] Restore Decodo as primary transcript provider. Rationale: GCT1 reversed the agreed cascade (YouTube→Decodo→Placeholder), but YouTube requires residential proxy that isn't reliably configured. Decodo now has real API key. Alternatives: Keep YouTube first (GCT1 order), build YouTube Data API v3 tier. Confirmed by user: yes
- [2026-06-19] [AGY3] [ACTIVE] [DECISION] Decompose 5 monoliths in Wave 6 using Ponytail guidelines. Rationale: Splitting SupabasePersistenceAdapter.ts (1206 LOC) into 4 adapters, rules.ts (1135 LOC) into config + engine, DashboardContainer.tsx (726 LOC) into clean UI sub-components, worker.ts (657 LOC) into routing + route files, and stripe/webhook/route.ts (517 LOC) into helper handlers. This reduces cognitive load and enforces strict single responsibility. Alternatives: Keep files as-is. Confirmed by user: no
- [2026-06-19] [Antigravity] [ACTIVE] [DECISION] Enable Ponytail mode and perform over-engineering review/audit. Rationale: User requested baseline audit and code review via Ponytail commands to minimize over-engineering before performing any codebase mutations. Alternatives: Normal/standard workflow. Confirmed by user: yes
- [2026-06-19] [Antigravity] [ACTIVE] [DECISION] Split monolithic PersistencePort into domain-focused ports. Rationale: Splitting monolithic PersistencePort (16 methods) into AnalysisPersistencePort, GraphPersistencePort, and BillingPersistencePort to adhere to Interface Segregation Principle (ISP) and improve domain boundaries. Alternatives: Keep monolithic interface. Confirmed by user: yes
- [2026-06-19] [Antigravity] [ACTIVE] [DECISION] Refactor services and adapters to resolve Hexagonal boundary violations (S1-9). Rationale: Invert dependencies between services and adapters/database. Domain services under web/lib/services/ should interact with the database via Port interfaces, not direct DB clients. Adapters should not import from the service/domain layer. Alternatives: Keep direct DB client calls in services and cross-layer imports. Confirmed by user: yes (requested in task prompt)
- [2026-06-20] [OCT2] [ACTIVE] [DECISION] Fail-closed on STREAM_HMAC_SECRET. Rationale: Removed static mock secret `'dev-hmac-secret-123'` from MOCK_DEFAULTS. If env var is missing, getter returns `undefined` — calling code must handle absence rather than silently using a forgeable default. Alternatives: Keep mock default (security hole on preview), hard-fail/throw (breaks Zero-Fatal policy). Confirmed by user: yes
- [2026-06-20] [OCT2] [ACTIVE] [DECISION] Decouple client disconnect signal from persist fetch. Rationale: Previously `c.req.raw.signal` was passed to both engine streaming (correct — stop on disconnect) and atomic-persist (incorrect — kills server-side write). Now persist uses a dedicated server-side AbortController with 15s timeout, immune to client navigation. Alternatives: Use `waitUntil` alone without abort (risk of hung persists), keep client signal bound (data loss on every navigate-away). Confirmed by user: yes
- [2026-07-07] [Claude] [ACTIVE] [DECISION] Chat Grounding Security Gate (ADR 008). Rationale: the chat's entire epistemic universe is the one bound video's analysis; if that analysis has no usable content (no transcript, failed, still processing), the use case must refuse — persist a controlled message, mint no stream token — rather than let the model answer from general knowledge. Root cause of the observed "fabricated recipe for a no-transcript video" leak. Alternatives: prompt-only mitigation (rejected — unenforceable, a model can still sample a confident wrong answer). Confirmed by user: yes (user explicitly framed the working-but-ungrounded chat as the bug, not a feature)
- [2026-07-07] [Claude] [ACTIVE] [DECISION] Chat Conversation↔Analysis Ownership Binding (ADR 009). Rationale: `POST /api/chat/conversations` wrote a client-supplied `analysisId` via the service-role client (RLS bypassed) with no ownership check — an IDOR letting any user bind a chat to another user's private analysis (root cause of the observed "double leak" — chat answering about a wrong/different video). Fixed with two layers: (1) route-level `verifyOwnership` check at creation, 404 on mismatch; (2) `getAnalysisGrounding` now optionally scoped by `userId`, so even a pre-existing cross-bound conversation resolves null grounding and the ADR 008 gate refuses. Alternatives: RLS-only enforcement (rejected — this path uses the service client specifically to bypass RLS for legitimate S2S writes elsewhere, so RLS gives zero protection here; the check must be explicit application code). Confirmed by user: yes
- [2026-07-07] [Claude] [ACTIVE] [DECISION] Dimension-0 Executive Digest — single idempotent cheap-cascade completion, uncounted (ADR 010). Rationale: one cheap non-streaming completion (`CHAT_CASCADE`, not the analysis cascade) over the assembled 11-dimension markdown produces a 3-tier digest (snapshot/takeaways/overview), generated lazily on first view of a completed full analysis, cached in `analyses.executive_digest jsonb` so it runs at most once ever per analysis (idempotent unless `force: true`). Rendered as "Dimension 0", uncounted against the 1..11 range (dimension_count, completeness, reaper all untouched). Alternatives: generate as a 12th stream during main synthesis (rejected per user direction — keep it strictly after-the-fact, on-demand, and decoupled from main synthesis cost/latency). Confirmed by user: yes ("cool, number 12")
- [2026-07-16] [Antigravity] [ACTIVE] [DECISION] Add a 4th tier to the Executive Digest for Detailed Summary. Rationale: The UI expects 4 tiers (Snapshot, Overview, Key Takeaways, Detailed Summary), but the LLM previously produced only 3, causing Overview and Detailed Summary to render identical content. Splitting them into a 1-2 paragraph Overview and a 3-5 paragraph Detailed Summary improves layout fidelity. Alternatives: Remove the 4th tier from the UI. Confirmed by user: yes

- [2026-08-29] [OC] [ACTIVE] [DECISION] Strict N<->N highlights-takeaway DAG enforcement (master dispatch 2026-08-29). Rationale: system prompt demanded parent_takeaway_idx 0..N-1 while the user message said null allowed -- conflicting instructions produced count drift (scrubber 11 vs table 8). Fixed: user message now overrides the null option (nearest plausible segment), parser dedupes by takeawayIdx (first wins) when takeawaysCount>0. Standalone mode (no takeaways) unchanged. Alternatives: fabricate missing highlights to force exact-N (rejected -- hallucinated timestamps), drop null-idx entries when takeaways exist (rejected -- data loss). Confirmed by user: yes (dispatch directive #1)
- [2026-08-29] [OC] [ACTIVE] [DECISION] Fail-soft Sentry auth failures on /api/admin/logs/sentry (master dispatch 2026-08-29). Rationale: expired/revoked SENTRY_LOGS_AUTH_TOKEN returned HTTP 500 and crashed the Admin Dashboard; 401/403 now return 200 {issues:[], warning} so the dashboard renders. Other failures keep the 500 error path. Alternatives: leave 500 + client-side handling (rejected -- dispatch directive). Confirmed by user: yes (dispatch directive #5)
- [2026-08-29] [OC] [ACTIVE] [DECISION] Restore EXECUTE grant for authenticated on admin_list_users_activity (master dispatch 2026-08-29). Rationale: live information_schema query proved authenticated lacks the grant (service_role/postgres only) -- grants drifted from 20260801084347; the admin users route intentionally calls as the authenticated caller, hence 42501. Migration revokes public/anon, grants authenticated+service_role, re-asserts SECURITY DEFINER. Confirmed by user: yes (dispatch directive #5)
- [2026-09-11] [OC] [ACTIVE] [DECISION] Remediation budget-drain guards: transcript-presence candidacy gate + failed-attempt retry burn. Rationale: 2026-09-01 live incident — the entire monthly $2.00 hardCap was burned in 35 minutes (88 usage_log events, 12 analyses), and every 5-minute remediation tick since silently no-op'd (BudgetExhausted returns 200 OK to QStash, so nothing alerted). Two stacked defects: (1) remediation_retry_count was only incremented on the StillPartial persist path, so rows whose worker calls persistently failed were retried unboundedly; (2) the retried population was dominated by rows whose transcripts had been purged (72h retention) or whose archived video_ids can never re-fetch — the worker's placeholder-transcript path yields zero usable dimension fragments by construction, so the spend was structurally unrecoverable. Fix: candidates without a transcripts row for their video_id are excluded up front (fail closed on query error); WorkerFailed/StitchFailed now burn one shared-ceiling retry through a new CAS-guarded recordRemediationFailure port method (same double-guard shape as tryRequeuePartial; under-counts by at most one on a concurrent collision, never over-counts). Known trade-off accepted: a real video_id whose transcript was purged but still re-fetchable from YouTube is also excluded — manual re-analyze is its recovery path (affects XMA9iZEUL0s, whose transcript is already purged). The bucket itself stays empty until the 2026-10-01 calendar reset by design (ADR 019's no-refill-within-period rule). Alternatives: refund-on-failure token reconciliation (rejected this pass — needs real-cost reconciliation, bigger change); fetch-transcript-before-candidate (rejected — pushes unrecoverable work into the hot path). Confirmed by user: yes (dispatch directive 2026-09-11, "Implement a real fix... do NOT just document the finding")
- [2026-09-11] [OC] [ACTIVE] [DECISION] PR #310 post-merge follow-ups: eligibility pagination (P1a), enforced failure-counter persistence via Redis quarantine (P1b), atomic owned-fields counter RPC (P1c). Rationale: post-merge review found 3 real correctness gaps in the merged guards. (P1a) findAnalysesWithMissingDimensions applied `limit` to RAW rows before the transcript/marity filters, so a page of transcript-purged rows starved every eligible candidate behind it indefinitely — now paginates (.range) until `limit` ELIGIBLE gaps are collected, capped at MAX_CANDIDATE_PAGES=5 (worst case 500 rows/sweep, the same scale analysis-reaper.ts's `limit ?? 500` already accepts); no FK analyses↔transcripts exists, so a PostgREST !inner embed was not cleanly available and pagination was chosen over a schema change. (P1b) recordFailure caught counter-write errors as "non-fatal", so a persistent Supabase write outage recreated the 2026-09-01 budget drain one layer down (counter never increments → unbounded retries) — the write is now retried with backoff (1s, 2s), and on total failure the row is quarantined in Redis (remediation:quarantine:<id>, TTL = new registry key remediation.quarantineTtlSeconds, fallback 21600s, seeded 20260911201100) and skipped by the harness pre-budget; CAS losses ({updated:false}) are deliberately NOT quarantined (another writer owns the row). Residual risk bounded: with Redis also down, the token bucket fails closed (executeRedisScript → -1), so unbounded spend remains impossible. (P1c) recordRemediationFailure overwrote the FULL validation_report snapshot guarded only by billing_status+retry_count, clobbering concurrent unrelated-field writers — replaced by the record_remediation_failure RPC (migration 20260911201000): one atomic UPDATE merging ONLY the owned fields into the row's current jsonb (jsonb_set), same text-compare retry CAS as the PostgREST OR-filter it replaces (absent-or-0 / exact N, fe88a805's edge case preserved), revoke from anon/authenticated/public per the 20260802125410 posture; buildRemediationFailurePatch deleted (the DB now owns the merge). Alternatives considered: updated_at-based CAS on the old full-write (sanctioned fallback — rejected: still ships the stale-snapshot write, only fails it closed; the RPC eliminates the clobber surface entirely); DB-level !inner transcript join (rejected: requires an FK migration). Confirmed by user: yes (dispatch directive 2026-09-11, docs/agent-prompts/2026-09-11-oc-pr310-review-findings.md — bug-fix exception to the confirm-before-implement rule applies; CC re-verifies before merge).
