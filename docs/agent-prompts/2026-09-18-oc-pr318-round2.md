# Dispatch — PR318 round 2 fixes (worktree oc-pr318-hallucination-followup, branch fix/dim2-dim4-hallucinated-credibility-name)

Target: OC (glm-5.3-flash, low effort). Ledger protocol mandatory: read .memory/AGENT_LEDGER.md (relative path), post IN_PROGRESS, then DONE.

This PR added grounding rules to `web/lib/prompts/ucis-v5.3.ts` Dimension 2.3/4.2 to stop the model inventing a named expert ("Jason Nadak" incident, verified real via live YouTube data + Supabase). Real gaps per external review:

1. **P0 CI**: `DeepSource: JavaScript (web)` failing on current head — find exact rule/file, fix root cause, rerun.
2. **P1 — safeguard only lives in the embedded fallback constant**: `resolvePromptTemplate()` (see `worker/src/services/PromptBuilder.ts`) can resolve a DB/Redis template or an explicit `promptOverride` that bypasses `UCIS_V5_3_SYSTEM` entirely — production analyses on non-default templates get zero protection. Trace every path from analysis request to final LLM prompt (embedded default, DB/Redis template resolution, `promptOverride`). Either apply the 2.3/4.2 grounding rules as a post-resolution injection (append to whatever template wins, before sending), or explicitly validate/reject an override that lacks them. Add a test proving the rule text survives every resolution path.
3. **P1 — 4.2's "established elsewhere in this analysis" is too broad**: a fabricated Dimension-2.3 entity can become 4.2's "trusted" source (cross-dimension poisoning). Narrow 4.2 to: authority-transfer references may cite ONLY names/roles/credentials explicitly present in Dimension 2.1 or directly observed in the transcript — never facts introduced by other GENERATED dimensions (2.3, 3, etc.). If no supported authority exists, omit the name entirely rather than inferring one.
4. **P1 — only "name" is guarded, not title/role/credential**: a real presenter could still get an invented title/role/affiliation ("educator", "advisor") with no support. Extend both 2.3 and 4.2's language to explicitly prohibit inventing OR embellishing names, titles, roles, affiliations, and credentials — every such attribute must trace to 2.1 or the transcript, or be omitted.
5. **P1 — no machine-checkable enforcement, prompt text only**: a model can ignore/partially-follow prose instructions. Add a lightweight post-generation validation step (can be a new pure function + a few tests, not a full pipeline rewrite unless trivial): extract person/credential mentions from Dimensions 2.3/4.2, cross-check against Dimension 2.1's extracted presenter name(s) + a simple presence-check in the transcript text; flag/strip unsupported claims. Use judgment on scope — a real, working, tested minimal version beats an elaborate unbuilt one; document what's out of scope in the report if you narrow it.
6. **P2 — zero regression tests for the fix itself**: add focused tests asserting the 2.3/4.2 prompt text contains the grounding/no-invention language (so a future edit can't silently regress it), covering default/DB-Redis/override prompt-resolution paths per item 2. Add one regression fixture (a transcript+metadata pair matching the real MoBr0nQtOnA shape) proving an unsupported "named expert" claim gets omitted, not fabricated — mock the LLM call if a real one isn't feasible in this test harness, but assert the ASSEMBLED PROMPT contains all required guards for that fixture's resolution path.

## Gates
```
pnpm --filter @hex-yt-intel/web exec tsc --noEmit
pnpm --filter @hex-yt-intel/web exec vitest run
pnpm --filter @hex-yt-intel/web lint
pnpm dlx tsx scripts/verify-quality-engine.ts --ci --compare
pnpm dlx tsx scripts/verify-quality-engine.ts --mode full --ci
pnpm exec tsx web/scripts/contract-auditor.ts
```
Skills: qa-intel/code-reviewer/simplify/review-delta/contract-auditor always; race-condition-guard NOT applicable; owasp-top-10 NOT applicable (no auth/secret path). Focus on prompt-injection/data-provenance thinking instead. Push to `fix/dim2-dim4-hallucinated-credibility-name`, verify `gh pr checks 318` clean. Do NOT merge. Report RCA→Fix→proof→gates→files per item.
