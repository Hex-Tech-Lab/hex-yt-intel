# Dispatch — PR317 round 2 fixes (worktree oc-wave-a-security-backlog, branch fix/wave-a-security-backlog)

Target: OC (glm-5.3-flash, low effort). Ledger protocol mandatory: read .memory/AGENT_LEDGER.md (relative path), post IN_PROGRESS, then DONE.

This PR bumps Next.js 16.2.11→16.3.3 (fixes 2 unauthenticated RCE CVEs) and scopes `output:'standalone'` off Vercel. Real leftovers per external review:

1. **P0 CI**: `DeepSource: JavaScript (web)` failing on current head — find the exact rule/file, fix root cause (don't suppress), rerun.
2. **P1 next.config.ts — unsafe VERCEL-detection fallback**: `process.env.VERCEL` is the sole signal; if hidden/unavailable, build silently falls back to `output:'standalone'` and can recreate the exact ENOENT packaging failure this PR just fixed. Harden: either fail loudly when deployment target is ambiguous, or use an explicit repo-controlled deploy-mode flag as the primary signal with `VERCEL` as a documented fallback, not the only check. Add tests: VERCEL unset / VERCEL=1 / VERCEL explicitly cleared.
3. **P1 eslint-config-next stuck at 16.2.6** while `next` is now 16.3.3 — check real compatibility; if they should track, bump `eslint-config-next` to match, regenerate lockfile, run lint+build. If intentional, add a comment explaining why + a compatibility guard test.
4. **P1 validation gap — treat as minor upgrade, not patch**: run the ACTUAL production build (not just tsc), plus `pnpm exec tsx scripts/enforce-bundle.mjs` (bundle enforcement, already wired into `pnpm run build`), and exercise representative routes/API handlers/middleware. Also correct the PR description's "same-minor-line patch bump" framing — 16.2.x→16.3.x is a minor upgrade, not a patch.
5. **P1 no security-regression coverage for the actual CVE fix**: build a small focused test hitting the Image Optimization API path (AVIF + non-AVIF Accept headers, malformed/oversized/unauthenticated inputs) confirming expected status codes, no unexpected execution path. If a full E2E image-optimization test is impractical in this test harness, do the closest feasible unit/integration-level check and say explicitly what's NOT covered.
6. **P2 clean frozen install verification**: run `pnpm install --frozen-lockfile` on a clean state, `pnpm why next` — confirm zero `16.2.11` references anywhere in the resolved graph.

## Gates
```
pnpm --filter @hex-yt-intel/web exec tsc --noEmit
pnpm --filter @hex-yt-intel/web run build
pnpm --filter @hex-yt-intel/web exec vitest run
pnpm --filter @hex-yt-intel/web lint
pnpm dlx tsx scripts/verify-quality-engine.ts --ci --compare
pnpm dlx tsx scripts/verify-quality-engine.ts --mode full --ci
pnpm exec tsx web/scripts/contract-auditor.ts
```
Skills: qa-intel/code-reviewer/simplify/review-delta/contract-auditor always; owasp-top-10 mandatory (this is a security CVE fix); vercel-optimize if touching build/deploy config further. Push to `fix/wave-a-security-backlog`, verify `gh pr checks 317` clean. Do NOT merge. Report RCA→Fix→proof→gates→files per item, plus explicitly flag anything NOT covered (e.g. no real Vercel deploy test possible from this sandbox).
