# StreamToken Type

The `StreamToken` is the signed streaming token handed to the client for direct browser→worker flow.

- **`sig`**: HMAC signature to verify token authenticity.
- **`exp`**: Expiration timestamp.
