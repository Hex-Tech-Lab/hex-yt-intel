# UI/UX Feedback Triage — 2026-08-20 (Cairo)

Raw feedback from live screenshots this session. Grouped by launch-blocking severity, not by arrival order. Status updated as fixes land — see per-item notes for commit/PR references.

## P0 — Real bugs, block launch

1. ✅ **FIXED (commit 60da21f6, PR #257).** Dimension 11 (Commercial Yield) table — last column fully broken. Root cause confirmed against live Supabase data: the LLM's table had a 3-column header but 4-column data rows; `preprocessMarkdown`'s repair logic rewrote the delimiter to match the (wrong, short) header instead of the real data width, leaving an unlabeled 4th column with no `table-layout:fixed` width that collapsed to near-zero and wrapped one character per line. Fixed in `web/lib/utils/format.tsx` to derive column count from the widest row and pad the header instead. Live-verified by re-rendering the actual corrupted content through the fix.
2. ✅ **FIXED (PR #257).** Dimension 8 (Semantic Foundation) numbered list — double-digit bullets wrap. Fixed via `ol.astryx-list > .astryx-list-item > span:first-child` CSS override in `web/app/globals.css`, scoped to `<ol>` only (automated review caught an initial unscoped version that would've widened bullet-list dot/circle markers too — fixed before merge).
3. ✅ **FIXED (PR #257).** Chat composer focus ring wraps the wrong element. Moved from `.astryx-chat-composer` (outer wrapper) to `.astryx-chat-composer-input` (the real input surface) in `web/app/globals.css`; border-radius also reduced per "a little less rounding."
4. ✅ **FIXED (PR #257).** Timestamp mentions not visually marked as links. Real root cause: `ApexSummaryCard.tsx` rendered raw content through `<Markdown>` with no `preprocessMarkdown()` call and no `#t=` → `TimestampLink` override (the pipeline that already worked correctly in `SelectedDimensionReadout.tsx`). Fixed by wiring the same pattern into `ApexSummaryCard.tsx`. Live-verified via isolated render, no hydration errors. Deliberately NOT extended to `StreamingGrid.tsx`'s clamped preview cards — that component is whole-card-clickable (opens the dimension drawer), and a nested `<a>` inside its click handler would be a worse regression than the one being fixed.

## P1 — Real feature gap, not just polish

5. ✅ **FIXED (PR #257, then hardened).** Highlights reel placement — moved from underneath Executive Summary to directly under the video player in `DashboardContainer.tsx`. Automated review caught a real regression in the first version (rendered even without `videoMetadata`, i.e. could show with no real player/duration context) — fixed by gating on `videoMetadata` explicitly, not just the broader `hasHadVideoRef` fallback the surrounding block already renders under.
   - **⚠️ User-reported regression (2026-08-20, live preview on PR #258, not yet root-caused):** "the highlight scrubber returned to old position" — needs investigation, see below.
6. ✅ **ADDRESSED (PR #258, dispatched subagent, base branch `fix/dimension-ui-p0-batch`).** Highlights reel over-compression — root cause was actually THREE stacked caps (prompt's "select 4-12 moments" instruction, `MAX_HIGHLIGHTS=12` constant, and an unset `maxTokens` silently falling back to a 2000-token default that could truncate the highlight array before the count cap even mattered). Replaced with Settings Registry keys `highlights.maxCount`(40)/`highlights.maxOutputTokens`(6000). Unit-test-verified: `parseHighlightsExtraction` now keeps 30 items where the old code capped at 12. **Not yet live-browser-verified against a real seeded analysis** (flagged by the agent as a deviation — no existing fixture, generating one needs a real LLM pass).
7. ⚠️ **PARTIALLY ADDRESSED (PR #258).** Chat/reel consistency — full data-source unification was NOT done (flagged as a deliberate scope decision by the agent, not a silent gap); `chat-grounding.ts` now at least explicitly documents why the two can differ instead of leaving it unexplained. Still an open item if full unification is wanted.
8. ✅ **ADDRESSED (PR #258).** Highlights reel UI redesign — built on `EntityMentionTimeline.tsx`'s visual shell only (marker track, active-window highlight, Prev/Next chrome), explicitly NOT its seek/mention-identification logic (which is separately broken, see the regression note filed by that PR). New `HighlightsTrack.tsx` + `useHighlightTicker.ts`, ticker-style script preview, Prev/Next, speed control (required adding `setPlaybackRate` through `VideoPlayerPort`/`YouTubePlayerAdapter`/`useVideoStore`/`VideoPlayerCard`). Top-right keypoint/duration/percent indicator kept as-is. **Not yet live-browser-verified** (same deviation as item 6). PR #258 not yet merged — awaiting CI/CodeRabbit/Cubic/Snyk review.

## Regressions found during highlights-reel work

- **`EntityMentionTimeline`'s slide-down scrubber UI reportedly not appearing at all in the live preview** (user report, 2026-08-20), despite the underlying `setSeekTo` still firing (to wrong/repeated targets — see below). Distinct from the seek-accuracy bug. Not yet root-caused — candidate: a condition on `timelineEntityData` in `DashboardContainer.tsx` never becoming truthy, a prop mismatch, or a silent render error. Flagged, not fixed (explicitly out of scope for the highlights-reel redesign task).
- **`EntityMentionTimeline`'s seek-target/mention-identification logic is broken** — clicking an entity jumps the video to wrong or repeated locations. Known, previously discussed, still unresolved. This is why the highlights-reel redesign was explicitly instructed to reuse only `EntityMentionTimeline`'s visual shell, never its seek logic.
- **Highlights reel reportedly reverted to its old position in the latest preview** (user report, 2026-08-20) — under investigation now. Candidate causes: PR #258 branched from `fix/dimension-ui-p0-batch` and may have touched `DashboardContainer.tsx` in a way that conflicts with or reverts item 5's placement fix, or the preview being viewed is a stale/different deployment than expected.

## P2 — Deferred, explicit user instruction

9. **KG / WordCloud / MindMap rendering issues** (monochrome purple, oversized/overlapping text in Mind Map nodes, KG legend shows 5 entity-type colors but graph renders in one color). Root cause candidate: content is being classified almost entirely as `object` type (visible in KG legend swatch matching WordCloud's purple), which is itself the deeper problem — user's real complaint is that entity typing (person/org/location/event/object) is not the valuable signal; it's a distraction from what should actually be highlighted. User explicitly said **not now** — "once you have the time" — but flagged as launch-relevant eventually and previously considered dropping KG entirely if not fixed. Also noted: renders correctly roughly 1-in-2 times, inconsistent across identical reloads — a real intermittent/race bug on top of the coloring issue, not just a style problem.
   - **Sub-finding, 2026-08-20:** the KG is also surfacing the 11 dimension section headers themselves as graph nodes — literally "APEX INTELLIGENCE (concept)", "PROVENANCE, METADATA & VIRALITY PROFILE (concept)", "CONTENT ARCHITECTURE & FIRST PRINCIPLES (concept)", etc., all 11 dimension titles, each tagged `(concept)`. These are structural section headings from the analysis output, not real extracted entities — likely the KG/entity extraction pass (or its client-side TF-IDF fallback, ADR 023) is treating dimension heading text as candidate entity spans instead of excluding headings from the extraction source text. Grouped with item 9, still parked, but this is a concrete, scoped sub-bug (probably a filter/exclusion-list fix at the extraction boundary) worth tackling first whenever KG work resumes — likely cheaper than the coloring issue and may reduce the "everything is `object`" noise too, since heading phrases would otherwise get dumped into the least-specific type bucket.
10. **Highlights-reel chapter-transition polish (explicitly deferred by user as a later marketing feature, not this pass):** brief fade-to-black + logo/watermark bump between highlight segments (~1s black, ~1s fade-in) for brand presence when content is shared/recorded. User suggested a possible round toggle button near the speed control to opt in/out. Log only — do not build without explicit go-ahead.

## Not yet investigated

- Whether items 4 (timestamp-link styling) and 7 (chat/highlights consistency) share a root cause with each other or with the TestSprite-discovered `Invalid appUrl callback destination` local-dev gap — unrelated on current evidence, no shared code path found.

---

**Recommended sequencing (my read, not yet confirmed with user):** P0 items 1-4 first (small, isolated CSS/rendering fixes, each independently testable, no design decisions needed). Item 5 (reposition highlights reel) is a one-line layout move, bundle with P0. Items 6-8 (highlights reel redesign + cap removal + chat/reel consistency) are a real multi-file feature project — needs its own scoped PR, likely its own design pass, and touches the segment-selection algorithm (not just UI). Item 9 stays parked per explicit instruction.
