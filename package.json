{
  "name": "hex-yt-intel",
  "version": "1.6.0",
  "private": true,
  "description": "YouTube Content Intelligence Platform",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "pnpm": "^11.1.3",
    "tailwindcss": "^4.3.0"
  },
  "engines": {
    "node": "24.16.0",
    "pnpm": "^11.1.3"
  }
}
