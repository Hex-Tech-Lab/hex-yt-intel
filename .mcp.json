{
  "mcpServers": {
    "testsprite": {
      "command": "pnpm",
      "args": ["dlx", "@testsprite/testsprite-mcp@latest"],
      "env": {
        "API_KEY": "${TESTSPRITE_API_KEY}"
      }
    }
  }
}
